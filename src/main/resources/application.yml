spring:
  profiles:
    active: dev
  mvc:
    view:
      suffix: .html
      prefix: /
  datasource:
    url: jdbc:mysql://121.4.43.41:3306/weixinBackEnd?setUnicode=true&characterEncoding=utf8&useSSL=false
    username: littleb
    password: mymmyyds
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml

server:
  port: 8888
